# use Command

```shell

 scp -r packages/interval-engine/app.yaml root@<ip>:/opt/interval/packages/interval-engine/app.yaml

docker run -p 5782:5782 -v /opt/interval-sign/packages:/usr/interval/packages --name="interval" -d ff2fb7041fc7
```

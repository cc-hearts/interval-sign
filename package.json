{
  "name": "interval",
  "version": "1.0.1",
  "description": "interval task",
  "main": "index.js",
  "scripts": {
    "start:engine": "pnpm --filter @repo/interval-engine start",
    "start:engine:ts": "pnpm --filter @repo/interval-engine start:ts",
    "build:engine": "pnpm --filter @repo/interval-engine build",
    "dev": "pnpm --filter @repo/interval-task dev"
  },
  "keywords": [
    "vue",
    "node"
  ],
  "author": "heart",
  "license": "MIT",
  "dependencies": {
    "typescript": "^4.6.4"
  }
}
